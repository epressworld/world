export const metadata = {
  title: "Ecosystem — epress",
  description:
    "Explore the epress ecosystem: tools, applications, and services built on open protocols.",
}

export default function EcosystemLayout({ children }) {
  return children
}
